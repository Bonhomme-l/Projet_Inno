<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetUp">
        <activity android:name=".AjouterActivity2"></activity>
        <activity android:name=".AjouterActivity" />
        <activity android:name=".InscrireActivity2" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".InscrireActivity1"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ProfilAnimalActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>